{
  "cli": {
    "version": ">= 5.0.0",
    "appVersionSource": "remote"
  },
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal",
      "channel": "development"
    },
    "preview": {
      "distribution": "internal",
      "channel": "preview",
      "android": {
        "// buildType": "APK rather than the default AAB — an AAB cannot be sideloaded, and a release attachment people can actually install is the point.",
        "buildType": "apk"
      },
      "ios": {
        "// simulator": "Builds without an Apple Developer account, but only runs in the Simulator. Drop this to produce an installable IPA (needs a paid account and credentials in EAS).",
        "simulator": true
      }
    },
    "production": {
      "channel": "production",
      "android": {
        "buildType": "app-bundle"
      },
      "autoIncrement": true
    }
  },
  "submit": {
    "production": {}
  }
}
